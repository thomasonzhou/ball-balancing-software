[pytest]
markers =
    math_helper: marks tests as math helper (deselect with '-m "not math_helper"')
    stew: marks tests as math for the stewart platform

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
